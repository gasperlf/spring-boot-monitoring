server:
  port: 8082

spring:
  application:
    name: api-finance-service
management:
  endpoints:
    enabled-by-default: true
    web:
      exposure:
        include: "*"
  trace:
    http:
      enabled: true
